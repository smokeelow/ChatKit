'use strict';

// Test Setup
var assert = require('chai').assert,
    tmp = require('tmp'),
    sinon = require('sinon'),
    grunt = require('grunt'),
    util = require('../tasks/lib/download')(grunt);

describe('The download library', function() {
  var server;

  before(function() {
    //server = sinon.fakeServer.create();
  });


  it('getFileList: should generate a valid fileList for default', function() {



  });

});